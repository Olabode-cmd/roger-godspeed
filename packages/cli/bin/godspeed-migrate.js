#!/usr/bin/env node
/**
 * Godspeed Migration Engine Executable.
 *
 * Exposes the CLI entry point mapping down into the compiled
 * jscodeshift orchestrator in the dist folder.
 */
import '../dist/index.js';
